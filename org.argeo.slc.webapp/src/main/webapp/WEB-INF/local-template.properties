# Rename this file to local.properties

# For in memory database switch following lines
sessionFactoryParentBean=slcDefault.dataSource.inMemoryTestHSQL
#sessionFactoryParentBean=slcServerDefault.sessionFactory.persistentHSQL

# URL of the persistent database
jdbcUrl=jdbc:hsqldb:file:/<path to a directory>/<db name>;shutdown=true
# Under Windows use the following form (with /!)
#jdbcUrl=jdbc:hsqldb:file:/C:/dev/test/dbSlc/db;shutdown=true

